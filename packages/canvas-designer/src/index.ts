// @invana/canvas-designer — the visual designer for @invana/canvas.
//
// Surface 1 (today): node template authoring, in `./templates`.
// Future surfaces — the studio shell + per-surface designers for layers / layouts /
// behaviours — will sit alongside `templates/` and host the matching
// `@invana/canvas-ui` editors. See CLAUDE.md.
export * from './templates';
