/**
 * `@invana/renderer-pixijs` — the PixiJS drawing backend for `@invana/canvas`.
 *
 * Scaffolded during the renderer split; populated in P6, when `primitives/`,
 * the `Application` bootstrap, the viewport binding, textures and fonts move
 * here and `@invana/canvas` becomes pixi-free.
 *
 * See `docs/renderer-split-design.md` §6.
 */

/** Placeholder so the package typechecks before P6 moves the drawing code in. */
export const RENDERER_PIXIJS_PLACEHOLDER = true;
