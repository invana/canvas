/**
 * Default canvas styling configuration
 */

/**
 * Default canvas background style
 */
export const DEFAULT_CANVAS_BACKGROUND = {
  color: 0xffffff,
  alpha: 1,
};

/**
 * Default canvas grid style
 */
export const DEFAULT_CANVAS_GRID = {
  enabled: false,
  size: 20,
  color: 0xe0e0e0,
  alpha: 0.5,
};

/**
 * Default canvas viewport settings
 */
export const DEFAULT_CANVAS_VIEWPORT = {
  minZoom: 0.1,
  maxZoom: 10,
  wheelZoomSpeed: 0.001,
};

/**
 * Default canvas interaction settings
 */
export const DEFAULT_CANVAS_INTERACTION = {
  dragEnabled: true,
  zoomEnabled: true,
  panEnabled: true,
  selectEnabled: true,
};
