import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Welcome" />

# @invana/canvas — Storybook

This storybook is being rebuilt against the new architecture (Layer / Behaviour / Layout / Renderer — see `architecture-proposal.md` at repo root).

The full inventory of stories from the previous storybook is preserved at `migration-storybook-inventory.md`. Each entry is a checklist item to port.
