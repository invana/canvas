# Function: changedPaths()

> **changedPaths**(`patches`): `string`[]

Top-level keys touched by a patch set — bounded cardinality, span/metric-safe.

## Parameters

### patches

readonly `Patch`[]

## Returns

`string`[]
