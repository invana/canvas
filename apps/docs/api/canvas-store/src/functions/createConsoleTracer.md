# Function: createConsoleTracer()

> **createConsoleTracer**(`log?`): [`Tracer`](../interfaces/Tracer.md)

A dep-free reference [Tracer](../interfaces/Tracer.md) that logs each span on `end()`.

## Parameters

### log?

(`name`, `attrs`) => `void`

## Returns

[`Tracer`](../interfaces/Tracer.md)
