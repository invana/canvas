# Type Alias: CameraTransform

> **CameraTransform** = [`CameraTransformValue`](../interfaces/CameraTransformValue.md)

An absolute camera transform — world (0,0) at `(x, y)` screen px, uniform `zoom`.
