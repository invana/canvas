# Type Alias: Polyline

> **Polyline** = `ReadonlyArray`\<`Point`\>

Defined in: [canvas/src/primitives/types.ts:51](https://github.com/invana/canvas/blob/ee4faae6c3fc997ca94ad6a644b0fbd178a59b99/packages/canvas/src/primitives/types.ts#L51)

Flat ordered list of points. Output of a `Router`; input to a `PathStyle`.
Also used as the densified form of a `Path` for hit-testing
(`samplePath(path)`).
