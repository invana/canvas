# Function: createConsoleMeter()

> **createConsoleMeter**(`log?`): [`Meter`](../interfaces/Meter.md)

A dep-free reference [Meter](../interfaces/Meter.md) that logs each record/add — for debugging,
tests, or a console-only harness. Not for production export.

## Parameters

### log?

(`name`, `value`, `attrs`) => `void`

## Returns

[`Meter`](../interfaces/Meter.md)
