# Type Alias: ElkPadding

> **ElkPadding** = `number` \| \{ `bottom?`: `number`; `left?`: `number`; `right?`: `number`; `top?`: `number`; \}

Symmetric or per-side padding around the graph. Mapped to `elk.padding`.
