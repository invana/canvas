# Function: parseTemplate()

> **parseTemplate**(`text`): `FreeformStructure`

Parse + minimally validate a saved template. Returns `null` on bad input.

## Parameters

### text

`string`

## Returns

`FreeformStructure`
