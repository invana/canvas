# Function: newElement()

> **newElement**(`type`, `id`): `CardElement`

A fresh element of the requested kind, placed near the card's top-left.

## Parameters

### type

`"image"` \| `"text"` \| `"circle"` \| `"rect"` \| `"line"`

### id

`string`

## Returns

`CardElement`
