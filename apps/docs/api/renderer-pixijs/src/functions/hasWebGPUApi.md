# Function: hasWebGPUApi()

> **hasWebGPUApi**(): `boolean`

Whether the WebGPU API surface is present (`navigator.gpu`). Cheap and
synchronous. This is the signal [canUseWebGPU](canUseWebGPU.md) gates on.

## Returns

`boolean`
