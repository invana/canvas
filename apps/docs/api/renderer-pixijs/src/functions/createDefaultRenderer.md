# Function: createDefaultRenderer()

> **createDefaultRenderer**(`opts`): [`PixiRenderer`](../classes/PixiRenderer.md)

Build the default pixi backend. `events` is the canvas-wide bus.

## Parameters

### opts

`object` & `Partial`\<[`PixiRendererOptions`](../interfaces/PixiRendererOptions.md)\>

## Returns

[`PixiRenderer`](../classes/PixiRenderer.md)
