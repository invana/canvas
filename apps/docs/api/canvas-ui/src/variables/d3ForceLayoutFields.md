# Variable: d3ForceLayoutFields

> `const` **d3ForceLayoutFields**: `FieldConfig`[]

The full D3ForceLayout field set as one grouped `FieldConfig[]` — the default
`fields` for `<D3ForceLayoutEditorPanel>`.
