# Variable: edgeLODFields

> `const` **edgeLODFields**: `FieldConfig`[]

`@invana/forms` field schema for the EdgeLODBehaviour editor. Field `name`s
match `EdgeLODFields` 1:1 so the generator's `options.<name>` paths line up
with `mapping.ts`.
