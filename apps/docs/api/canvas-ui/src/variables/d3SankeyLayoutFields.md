# Variable: d3SankeyLayoutFields

> `const` **d3SankeyLayoutFields**: `FieldConfig`[]

The full D3SankeyLayout field set as one grouped `FieldConfig[]` — the default
`fields` for `<D3SankeyLayoutEditorPanel>`.
