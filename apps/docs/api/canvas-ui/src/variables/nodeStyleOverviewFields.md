# Variable: nodeStyleOverviewFields

> `const` **nodeStyleOverviewFields**: `FieldConfig`[]

`@invana/forms` schema for [NodeStyleOverviewEditorPanel](../../../canvas/src/variables/SpecStore.md) — a single colour
control. The field `name` matches [NodeStyleOverviewFields](../interfaces/NodeStyleOverviewFields.md) 1:1, so the
generator's `overview.color` path lines up with `mapping.ts`.
