# Type Alias: BackgroundLayerFields

> **BackgroundLayerFields** = [`BackgroundLayerOptions`](../interfaces/BackgroundLayerOptions.md)

Flat form-field shape. Matches [BackgroundLayerOptions](../interfaces/BackgroundLayerOptions.md) 1:1 here —
colours are already scalar strings, so no re-encoding beyond the
`number ⇄ #rrggbb` bridge the mapping applies for seed values.
