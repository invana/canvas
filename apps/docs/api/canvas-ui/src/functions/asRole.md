# Function: asRole()

> **asRole**(`value`): `ColorRole`

Defined in: canvas-ui/src/editors/field-helpers.ts:37

Narrow a form select value back to a `ColorRole` (empty → `undefined`).

## Parameters

### value

`string`

## Returns

`ColorRole`
