# Function: asRole()

> **asRole**(`value`): `ColorRole`

Narrow a form select value back to a `ColorRole` (the `(none)` sentinel → `undefined`).

## Parameters

### value

`string`

## Returns

`ColorRole`
