# Function: keyboardCameraOptionsToForm()

> **keyboardCameraOptionsToForm**(`o?`): [`KeyboardCameraFields`](../interfaces/KeyboardCameraFields.md)

Map a `KeyboardCameraInputBehaviourOptions`-shaped patch to the flat
[KeyboardCameraFields](../interfaces/KeyboardCameraFields.md) the `@invana/forms` generator renders. A 1:1 copy
— `keymap` is intentionally dropped (not editable here).

## Parameters

### o?

[`KeyboardCameraOptions`](../interfaces/KeyboardCameraOptions.md) = `{}`

## Returns

[`KeyboardCameraFields`](../interfaces/KeyboardCameraFields.md)
