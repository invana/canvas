# Function: geometricLayoutFields()

> **geometricLayoutFields**(`values?`): `FieldConfig`[]

The full GeometricLayout field set as one grouped `FieldConfig[]` — the
default `fields` for `<GeometricLayoutEditorPanel>`. Layout numerics vary with the
current `mode`.

## Parameters

### values?

[`GeometricLayoutFields`](../interfaces/GeometricLayoutFields.md) = `{}`

## Returns

`FieldConfig`[]
