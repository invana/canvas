# Function: roleField()

> **roleField**(`name`, `label`): `FieldConfig`

A `select` field bound to the colour-role vocabulary.

## Parameters

### name

`string`

### label

`string`

## Returns

`FieldConfig`
