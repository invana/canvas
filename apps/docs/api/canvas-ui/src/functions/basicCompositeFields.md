# Function: basicCompositeFields()

> **basicCompositeFields**(`_values?`): `FieldConfig`[]

The **Basics** scalar field set — the single card body fill colour, shown
up-front and ungrouped. Everything else (body size / stroke / clip, the root
silhouette) lives in [advancedCompositeScalarFields](advancedCompositeScalarFields.md). A function of the
values for symmetry with the advanced set.

## Parameters

### \_values?

[`CompositeScalarFields`](../interfaces/CompositeScalarFields.md) = `{}`

## Returns

`FieldConfig`[]
