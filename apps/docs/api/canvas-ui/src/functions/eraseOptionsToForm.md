# Function: eraseOptionsToForm()

> **eraseOptionsToForm**(`o?`): [`EraseFields`](../interfaces/EraseFields.md)

Map an `EraseBehaviourOptions`-shaped patch to the flat [EraseFields](../interfaces/EraseFields.md)
the `@invana/forms` generator renders. A direct pass-through — the only
option is the `target` enum.

## Parameters

### o?

[`EraseOptions`](../interfaces/EraseOptions.md) = `{}`

## Returns

[`EraseFields`](../interfaces/EraseFields.md)
