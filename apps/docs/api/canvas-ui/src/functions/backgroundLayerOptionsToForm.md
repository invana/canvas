# Function: backgroundLayerOptionsToForm()

> **backgroundLayerOptionsToForm**(`o?`): [`BackgroundLayerOptions`](../interfaces/BackgroundLayerOptions.md)

Map a `BackgroundLayerOptions`-shaped patch to the flat
[BackgroundLayerFields](../type-aliases/BackgroundLayerFields.md). Colours are normalised to strings; everything
else passes through.

## Parameters

### o?

[`BackgroundLayerOptions`](../interfaces/BackgroundLayerOptions.md) = `{}`

## Returns

[`BackgroundLayerOptions`](../interfaces/BackgroundLayerOptions.md)
