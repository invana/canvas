# Function: formToBinding()

> **formToBinding**(`values`): `NodeTypeBinding`

Read the form back into a `NodeTypeBinding` (drops empty rows).

## Parameters

### values

[`NodeStructureFormState`](../interfaces/NodeStructureFormState.md)

## Returns

`NodeTypeBinding`
