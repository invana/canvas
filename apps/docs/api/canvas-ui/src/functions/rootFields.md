# Function: rootFields()

> **rootFields**(`kind`): `FieldConfig`[]

Root-tab fields for the current `rootKind` (kind select + its geometry).

## Parameters

### kind

[`CompositeRootKind`](../type-aliases/CompositeRootKind.md)

## Returns

`FieldConfig`[]
