# Function: modeFields()

> **modeFields**(`mode`): `FieldConfig`[]

Per-mode geometry fields for the current mode. Dynamic: grid/snake show the
column + gap numerics, circular shows radius/spacing/angle.

## Parameters

### mode

[`GeometricLayoutMode`](../type-aliases/GeometricLayoutMode.md)

## Returns

`FieldConfig`[]
