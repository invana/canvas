# Function: backgroundLayerFields()

> **backgroundLayerFields**(`values?`): `FieldConfig`[]

The full BackgroundLayer field set as one grouped `FieldConfig[]` — the
default `fields` for `<BackgroundLayerEditorPanel>`. The Pattern section is elided
for a solid fill.

## Parameters

### values?

[`BackgroundLayerOptions`](../interfaces/BackgroundLayerOptions.md) = `{}`

## Returns

`FieldConfig`[]
