# Function: parallelEdgeOptionsToForm()

> **parallelEdgeOptionsToForm**(`o?`): [`ParallelEdgeOptions`](../interfaces/ParallelEdgeOptions.md)

Map a `ParallelEdgeBehaviourOptions`-shaped patch to the flat
[ParallelEdgeFields](../type-aliases/ParallelEdgeFields.md) the `@invana/forms` generator renders. All fields
are scalar / enum / boolean, so this is a straight pass-through.

## Parameters

### o?

[`ParallelEdgeOptions`](../interfaces/ParallelEdgeOptions.md) = `{}`

## Returns

[`ParallelEdgeOptions`](../interfaces/ParallelEdgeOptions.md)
