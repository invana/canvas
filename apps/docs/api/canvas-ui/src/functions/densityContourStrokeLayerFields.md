# Function: densityContourStrokeLayerFields()

> **densityContourStrokeLayerFields**(`values?`): `FieldConfig`[]

The full DensityContourStrokeLayer field set as one grouped `FieldConfig[]` —
the default `fields` for `<DensityContourStrokeLayerEditorPanel>`. The palette /
swatch inputs swap on the `strokePalette` toggle.

## Parameters

### values?

[`DensityContourStrokeLayerFields`](../interfaces/DensityContourStrokeLayerFields.md) = `{}`

## Returns

`FieldConfig`[]
