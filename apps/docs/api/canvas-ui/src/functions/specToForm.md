# Function: specToForm()

> **specToForm**(`spec?`): [`CardSpecFields`](../interfaces/CardSpecFields.md)

Seed the form from a serializable card spec (use as `defaults`).

## Parameters

### spec?

`HoverElementPreviewCardSpec` = `{}`

## Returns

[`CardSpecFields`](../interfaces/CardSpecFields.md)
