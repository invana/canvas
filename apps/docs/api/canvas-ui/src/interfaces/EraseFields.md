# Interface: EraseFields

Flat form-field shape the `@invana/forms` generator renders — one `target`
select. 1:1 with [EraseOptions](EraseOptions.md) (no nesting to flatten).

## Properties

### target?

> `optional` **target?**: `EraseTargetKind`
