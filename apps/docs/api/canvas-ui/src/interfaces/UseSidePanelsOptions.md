# Interface: UseSidePanelsOptions

## Properties

### defaultOpenId?

> `optional` **defaultOpenId?**: `string`

Which panel starts open. Default `null` (none).

***

### section?

> `optional` **section?**: `Pick`\<[`SpecStore`](../../../canvas/src/variables/SpecStore.md), `"defaultSize"` \| `"minSize"` \| `"maxSize"` \| `"collapsible"`\>

Region-size defaults applied to every panel unless its def overrides them.
