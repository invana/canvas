# Function: useGraphCanvas()

> **useGraphCanvas**(): `GraphCanvas`

Read the [GraphCanvas](../../../canvas/src/variables/SpecStore.md) from context. Throws when used outside a
`<Canvas>` so misuse fails loudly during render.

## Returns

`GraphCanvas`
