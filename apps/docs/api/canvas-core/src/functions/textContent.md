# Function: textContent()

> **textContent**(`style`): [`LabelContent`](../type-aliases/LabelContent.md) & `object`

Extract plain-text `LabelContent` from a label decoration style, if any.

## Parameters

### style

`unknown`

## Returns

[`LabelContent`](../type-aliases/LabelContent.md) & `object`
