# Function: strokePaint()

> **strokePaint**(`stroke`, `widthScale?`): `Record`\<`string`, `string` \| `number` \| `undefined`\>

Resolve a [ShapeStroke](../interfaces/ShapeStroke.md) to SVG stroke attributes. `widthScale` mirrors LOD scaling.

## Parameters

### stroke

[`ShapeStroke`](../interfaces/ShapeStroke.md)

### widthScale?

`number` = `1`

## Returns

`Record`\<`string`, `string` \| `number` \| `undefined`\>
