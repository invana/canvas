# Function: attrs()

> **attrs**(`map`): `string`

Serialise an attribute map, skipping `undefined` / empty values.

## Parameters

### map

`Record`\<`string`, `string` \| `number` \| `undefined`\>

## Returns

`string`
