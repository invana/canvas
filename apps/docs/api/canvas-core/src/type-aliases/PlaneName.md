# Type Alias: PlaneName

> **PlaneName** = `"backdrop"` \| `"content"`

Paint stripes. `plane` picks the stripe, `zIndex` orders within it.

Part of the pixi-free spec vocabulary — see `docs/renderer-split-design.md`.
