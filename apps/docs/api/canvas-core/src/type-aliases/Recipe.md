# Type Alias: Recipe\<T\>

> **Recipe**\<`T`\> = (`draft`) => `void`

An immer recipe — mutate the draft; the produced state is structurally shared.

## Type Parameters

### T

`T`

## Parameters

### draft

`T`

## Returns

`void`
