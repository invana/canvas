# Type Alias: Update\<T\>

> **Update**\<`T`\> = [`Recipe`](Recipe.md)\<`T`\> \| [`DeepPartial`](DeepPartial.md)\<`T`\>

A write: either a recipe (mutate the draft) or a declarative deep-partial patch.

## Type Parameters

### T

`T`
