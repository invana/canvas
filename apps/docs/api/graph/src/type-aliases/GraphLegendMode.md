# Type Alias: GraphLegendMode

> **GraphLegendMode** = `"auto"` \| `"light"` \| `"dark"`

Mode selector for light/dark colour resolution. `'auto'` follows the active
theme published on `ctx.theme`; `'light'` / `'dark'` pin explicitly.
