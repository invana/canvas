# Type Alias: GraphClipboardEventMap

> **GraphClipboardEventMap** = `object`

Event-map for [GraphClipboard.events](../classes/GraphClipboard.md#events).

## Properties

### change

> **change**: `object`

Fired whenever the buffer's contents change (copy / clear). Drives "can paste".

#### hasContent

> **hasContent**: `boolean`
