# Type Alias: GraphElementKind

> **GraphElementKind** = `"node"` \| `"edge"`

Defined in: [graph/src/store/types.ts:17](https://github.com/invana/canvas/blob/ee4faae6c3fc997ca94ad6a644b0fbd178a59b99/packages/graph/src/store/types.ts#L17)

Which kind of graph element — the canonical `'node' | 'edge'` discriminator,
shared across the graph package instead of each behaviour rolling its own.
