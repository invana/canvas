# Type Alias: MiniMapMode

> **MiniMapMode** = `"auto"` \| `"light"` \| `"dark"`

Mode selector for light/dark colour resolution. `'auto'` follows the host's
`prefers-color-scheme` media query; `'light'` / `'dark'` pin explicitly.
