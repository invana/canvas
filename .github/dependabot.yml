version: 2
updates:
  # Keep the GitHub Actions used in publish.yml / deploy-storybook.yml current,
  # so new major tags (and their upstream security fixes) land via a reviewable PR.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    groups:
      actions:
        patterns: ["*"]
