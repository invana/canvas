# Changelog

All notable changes to the `@invana/*` packages (lockstep versioned).
This file is generated by [git-cliff](https://git-cliff.org) on each release.

## [0.0.13] - 2026-09-11

### Breaking Changes
- **(canvas-ui,storybook)** Styling + selection view panels, one theme owner, folding tab strips

### Bug Fixes
- **(storybook)** An inactive tab drops its label, so the `…` menu can read its name

### Documentation
- **(rfcs)** A folded tab's overflow row renders its strip label, not its name

## [0.0.12] - 2026-09-09

### Breaking Changes
- **(graph)** Collapse is a node state, and each node owns its closed look
- **(canvas,store,graph)** Make lifting an element a projection of state
- **(storybook)** Flatten every story to a single sidebar leaf
- **(graph-datasets)** A dataset is a folder — data.ts + settings.ts
- **(storybook)** Flatten usecases/apps to one story per surface
- **(graph-datasets)** Drop the CanvasData / CanvasSettings aliases
- **(graph-datasets)** One data.ts per dataset, no record types
- **(storybook)** Lift the product surfaces out of usecases/apps
- **(graph-datasets)** Replace licence-encumbered datasets with seeded generators
- **(storybook)** Split usecases/ into tools/ and by-casestudies/
- **(graph)** ColorByBehaviour — colour by any field, as a category or a range
- **(graph)** ColorByBehaviour introspection, and mode 'category' -> 'categorical'
- **(graph)** `type` is required on GraphNode and GraphEdge

### Features
- **(canvas-ui)** Settings editors for WheelZoom / BackgroundLayer / GeometricLayout
- **(canvas)** WebGPU render-crash fallback to WebGL
- **(canvas)** Live setOptions on Behaviour base + CanvasSettingsBrowser panel
- **(canvas-ui)** Settings editor for every Behaviour / Layer / Layout
- **(canvas-ui)** Top-aligned editor labels; drop accordion-box padding
- **(canvas)** Composite `icon` part (glyph/svg/svg-url insets)
- Composite sub-part interactivity, clip, and built-in card node types
- **(graph)** First-class per-element visibility (hide/show) for nodes & edges
- **(graph)** Group visibility (hide/show container + subtree) + hidden-groups helpers
- **(canvas-ui)** LayersPanel view + useGraphEvent hook + visibility stories
- LayersPanelView groups + d3-force cluster + ELK compound-group layout
- **(canvas-ui)** Split group visibility into frame-only vs group+members
- **(graph-datasets)** Add Game of Thrones property-graph dataset
- **(telemetry)** OTLP observability — FPS metrics, traces & logging toggles
- **(graph-datasets)** Add graph schema (meta-graph) to Game of Thrones JSON
- **(graph-datasets)** Add Wikipedia data-viz cartography dataset
- **(graph)** Add hoverEdges option to HoverActivateBehaviour
- **(storybook)** Add LayersPanelView Wikipedia data-viz story
- **(canvas)** Shape content-visibility toggles for zoom-LOD
- **(graph)** Content zoom-LOD split into Text/Icon/Image behaviours
- **(storybook)** Wikipedia story — content LOD, centrality labels, degree sizing, fast load
- **(graph)** NodeCentralityBehaviour scales the label with the node
- **(graph)** NodeScaleLODBehaviour preserveRelativeSize mode
- **(graph)** Weighted degree on NodeCentralityBehaviour
- **(canvas-ui)** CanvasSettingsPanelView — one JSON-driven panel over the canvas definition
- **(graph)** EdgeLODBehaviour — thin edges below a zoom threshold
- **(canvas-ui)** Store-driven settings panel synced with the header
- **(telemetry)** CreateHttpMeter sink + DevInfoLayer phase breakdown
- **(canvas-react)** Rebuild GraphCanvasApp on AppLayoutV2 with side regions
- **(canvas-ui)** Add CanvasPagesTabbedView — Bootstrap-style page tabs
- **(graph,canvas-ui)** MiniMapLayer out-of-viewport mask + editor controls
- **(canvas-ui)** Make CanvasPagesTabbedView tabs stylable
- **(canvas-react)** Make GraphControlsToolbar edit/view sections trimmable
- **(canvas-react)** Add triggerLabelOnly for RichSelect toolbar items
- **(canvas-react)** Segmented ToggleGroup display for select toolbar items
- **(canvas-react)** Move layout picker to far right of GraphControlsToolbar
- **(canvas)** Add config.fitOnLoad + canvas.fitView() one-shot centre-on-load
- **(canvas-react)** GraphCanvasApp centres on load; add graph-canvas-app story variations
- **(canvas-react)** ClickViewBehaviour onClick alias + section minSize defaults to 0px
- **(storybook)** FullFeatured drives a docked right inspector via onClick
- **(storybook)** LiveSettingsEditors docks the panel in the app right region with a header toggle
- **(storybook)** Drop the minimap toggle from LiveSettingsEditors header
- **(storybook)** Split the Standalone patch + definition into two columns
- **(canvas-react)** Run-layout button toggles to Stop while a layout runs
- **(canvas)** Reactive layout-run state + no-drift, view-following animated d3-force
- **(canvas-ui)** NodeStyleEditor dispatcher + NodeStyleOverviewEditor colour editor
- **(canvas-ui)** Basic-first / advanced-opt-in node style editors
- **(storybook)** 3 ribboned canvas-react roots stories (Basic/WithTelemetry/Advanced)
- **(canvas-ui)** Standard GraphContextMenu + reusable HiddenElementsView
- **(canvas-ui)** SchemaViewer + SchemaToolbar + CanvasToolbar
- **(schema)** Hover-highlight neighbours + expose SchemaViewer layer id & children
- **(canvas-ui)** FindInCanvasViewPanel — structured find-in-canvas
- **(canvas-ui)** UseSidePanels — activity-bar controller for GraphCanvasApp side panels
- **(canvas)** Add TabbedRectShape — a folder silhouette for group frames
- **(storybook)** Make the Invana architecture diagram theme-responsive
- **(layouts)** Group-aware layouts — containment beyond ELK
- **(canvas)** Let a shape define its own minimal + content-fitted form
- **(graph)** Double-click a group frame to toggle it, and re-centre after
- **(storybook,canvas-react)** Compose every usecase story from GraphCanvasApp
- **(storybook)** Add the Style Designer usecase app
- **(graph,canvas-ui)** Add GraphLegendLayer with click-to-toggle types
- **(storybook)** Dataset switcher for the GraphVisualiser story
- **(graph-datasets)** Per-type generators + a card look for twitter
- **(canvas)** Hide background pattern below a zoom threshold
- **(storybook)** Code-explainability case study on the repo's own dataflow graph
- **(canvas,graph)** Name every container and graphic in the pixi scene tree
- **(canvas,renderer-pixijs)** Split the drawing backend out of the engine
- **(canvas-core,canvas,canvas-store,renderer-pixijs)** Extract @invana/canvas-core and restructure the engine packages
- **(canvas-core,canvas-store)** Invert the stack — canvas-core is the dependency-free floor
- **(canvas-core,canvas-store,canvas)** Pin the public API surface; one export route per name
- **(renderer-pixijs,graph-layouts)** Detach backends and layouts from the engine — canvas-core peers only
- **(engine)** Detach the pixi backend and land the v0.0.12 release prep

### Bug Fixes
- **(canvas)** Non-invasive WebGPU render guard; render nodes+labels in stories
- **(d3-force)** GetOptions() + re-apply on setOptions so live edits take effect
- **(canvas-ui)** Single-column settings editor forms (columns={1})
- **(canvas-ui)** LayersPanel populates on mount; story uses a right-side toggle
- **(graph)** Large-graph interaction perf — hover freeze + minimap zoom
- **(graph)** Repair frame-flush races on initial load (double-install + fit zoom)
- **(canvas)** Float raised (hovered/selected) set above all content
- **(canvas-react)** Clip GraphCanvasApp so the shell can't overflow its box
- **(canvas)** Don't let a canvas destroy clear the shared TexturePool
- **(security)** Patch prototype pollution, URL XSS/SSRF, and vulnerable deps
- **(canvas-react)** Size-aware default force collision in GraphCanvasApp
- **(graph-layout-d3-force)** Live sim settles + composite nodes don't overlap
- **(graph)** Clamp minimap viewport indicator to the minimap box
- **(storybook)** Enable remark-gfm so MDX docs render GFM tables
- **(graph)** Repaint node fill on clear + robust colour-by-label restore
- **(graph)** Raise a hovered group's contents, not the frame
- **(graph)** Defer edge installs until endpoint shapes exist
- **(graph)** Keep group options through a collapse/expand toggle
- **(graph)** Don't let ColorByLabelBehaviour clobber a consumer's fill resolver
- **(canvas)** Keep the auto-fit's layout subscriptions when activeLayout lands late
- **(graph,elkjs)** Make group frames enclose their members, at the configured spacing
- **(storybook)** Make the code-explainability look switcher actually switch
- **(graph)** Give card nodes a border, and keep row cells inside the card
- **(canvas,graph)** Group frames paint below the edges between their members
- **(graph,canvas-core)** GetBounds derives from the store — fitOnLoad stops framing the wrong box

### Performance
- **(graph)** Cache node render bounds; layouts read boundingBox
- **(canvas)** Viewport culling of off-screen shapes/connectors
- **(graph)** Frame-coalesced flush for the GraphLayer store
- **(canvas)** Memoise edge hit-test polyline (D)
- **(graph)** Hover highlight fast-path (G)
- **(canvas)** Stable hover pick — hysteresis + node-incidence bias (I/J)
- **(graph)** Rbush + viewport-scope LabelCollisionBehaviour
- **(graph)** Throttle LabelCollision passes during gestures
- **(storybook)** Register EdgeLODBehaviour on the Game of Thrones graph
- **(canvas)** Intern decoration labels, name inset children by role

### Refactors
- **(graph)** Make built-in card node types spec-driven classes
- **(storybook)** Propagate LabelResolutionLODBehaviour → TextResolutionLODBehaviour rename
- Rename node size behaviours for clear data-vs-camera boundaries
- **(canvas-react)** Split <Canvas> (base) + <GraphCanvas> (graph); forward telemetry
- **(storybook)** Fold Live Settings Editors into the CanvasSettingsPanelView story
- **(canvas-ui)** Rename CanvasSettingsPanelView → CanvasSettingsEditor, render sections as PanelStack
- **(ui)** Split canvas-react (headless) / canvas-ui (UI kit) + engine `kind`
- **(storybook)** Give each package its own top-level story namespace
- **(storybook)** Slim canvas-react/ namespace to its own React roots
- **(storybook)** One-story-per-file canvas-react roots + telemetry-URL demo
- **(storybook)** Drop DevInfoLayer overlay from canvas-react stories
- **(hover-preview)** Split HoverElementPreviewBehaviour across packages
- **(storybook)** Reorg stories to package taxonomy + canvas-react story rules
- **(storybook)** Remove StreamingDemo-based layout stories
- **(storybook)** Canvas-ui stories on GraphCanvasApp, dock into right region
- **(canvas-ui)** Overlay toggle buttons + self-wiring LayersPanelView
- **(schema)** Move schema to @invana/graph, self-wire theme, split stories
- **(canvas-ui)** Rename view surfaces to *ViewPanel, views/ → view-panels/
- **(storybook)** Standardize view-panel stories on the useSidePanels pattern
- **(storybook)** Standardize CanvasSettingsEditor story on useSidePanels
- **(canvas-ui)** EditorPanel suffix + group editors by engine surface
- **(canvas-ui)** Move editor-panels out to a top-level src/ folder
- **(canvas-ui)** Fold CanvasSettings into one store-connected EditorPanel
- **(storybook)** Style the architecture diagram by node type
- **(storybook)** Split usecases into product apps and domain verticals
- **(storybook)** Rename the settings-panel story to match its panel
- **(storybook)** File the GraphLegendLayer story by concept, flatten its sidebar leaf
- **(storybook)** Declare dataset payload types at the point of use
- **(storybook)** Colour Les Mis by type instead of a hand-rolled palette

### Documentation
- Node styling unification plan
- Add large-graph rendering performance plan
- **(perf)** Mark viewport culling / edge-LOD / frame-flush shipped in large-graph plan
- Plan for store-bound node-style editing (useNodeStyleEditor)
- **(storybook)** Document packages + layer/layout/behaviour architecture on Welcome page
- **(canvas-ui)** Make FindInCanvasViewPanel the reference view-panel story template
- Plan the render-intent pipeline + large-graph load path
- Add the code-structure ontology + the paint-order / restyle plans
- Diagnose a pasted error before fixing it (rule 14)
- **(graph-datasets)** Add a package README + the folder-exports plan
- RFC for the fitOnLoad "nodes aren't rendering" bug
- RFCs for the casestudies namespace, the new behaviours, and ColorByBehaviour
- **(rfc)** Spell out what ColorByBehaviour can colour by, and cap cardinality
- RFC for making `type` required on the stored graph record
- RFC for collapsing the node/edge record types down to two
- **(rfcs)** RFC per feature or fix, written before the code

## [0.0.11] - 2026-07-06

### Features
- **(canvas-react)** Reusable ExportPanel + hover-card ExportToolbar
- **(canvas)** Full-state JSON export/import (data + view + styling)

### Refactors
- **(canvas-react)** Namespace image export as *ImageExport / ExportImage*

## [0.0.10] - 2026-07-06

### Features
- **(export)** Viewport screenshot + true vector SVG export
- **(export)** Add aspectRatio option + interactive export story

## [0.0.8] - 2026-07-02

### Features
- **(layout)** Off-thread d3-force settle, render perf fixes, layout run events
- **(storybook)** Reusable StoryGraphApp shell for canvas-react stories
- **(storybook)** Generic StoryCanvasShell core; all canvas-react stories on it
- **(storybook)** Built-in dev-overlay toggle on the shell; tidy canvas-react stories
- **(storybook)** Shell collapses empty header/footer rails; NoChrome story
- **(canvas-react)** GraphCanvasLiteApp + GraphControlsToolbar; toolbars bake their own icons
- **(canvas-react)** GraphCanvasApp — one config-driven composable graph app
- **(canvas-react)** Host-provided theme for GraphCanvasApp; dev/minimap hooks, ElementDetailViewer, ThemeToggle
- **(canvas-react)** GraphCanvasApp chrome — balanced header regions, rail padding, message pickup
- **(storybook)** MultipleApps story — four independent GraphCanvasApps in a grid
- **(canvas)** HoverElementPreviewBehaviour — hover preview cards for nodes/edges
- **(graph)** Snappier HoverElementPreviewBehaviour defaults (openDelay/closeDelay = 50ms)
- **(hover-preview)** Per-type cards (serializable) + canvas-ui card editor
- **(canvas-react)** Element detail views + extensible property rendering
- **(theme+templates)** Signal-based theming + node structure/styling templates
- **(graph)** Free-form node card templates
- **(canvas-designer)** WYSIWYG node card designer package
- **(graph-datasets)** Twitter activity dataset
- **(storybook)** Card designer studio on the twitter feed
- **(canvas-designer)** Node-type template list + free-text field binding
- **(canvas)** Composite node cards with arbitrary silhouettes + ellipse shape
- **(storybook)** Hover preview, detail dock + theme switcher on composite-nodes story
- **(canvas-core)** Renderer-free state/events/telemetry/history kernel
- **(canvas-store)** Graph data (LayerData), named action API, event taxonomy
- **(storybook)** Canvas-store Playground — headless kernel test harness
- **(canvas-store)** Pluggable FlushMode trigger + data/event-flow & renderer-split plans
- **(canvas-store)** Phase 1 fast lane — typed-array positions, theme/geom, inventory + migration plan
- **(canvas)** M0 — engine sits on the @invana/canvas-store kernel
- **(canvas-store)** D13 3.1a — DataSource interface + setSource registration
- **(graph)** D13 3.1b — GraphStore implements DataSource
- D13 3.2/3.3 — GraphLayer registers its store; engine drives data flush
- **(canvas-store)** Tracing adapters + durationMs; playground exercises all events + traces
- **(canvas-store)** OTel tracing decorator + full-flow tap tracing; wire Playground to real ops → HyperDX
- **(canvas-store)** Finalize kernel for renderer split — IRenderer seam + one bus + view-as-truth
- **(canvas-store)** Converge engine onto the kernel bus + store (M0–M2 groundwork)

### Bug Fixes
- **(graph)** Minimap mirrors canvas faithfully (theme + scaled edges)
- **(canvas)** Trust navigator.gpu for WebGPU; drop WebKit guard
- **(hover-preview)** Opaque card surface, hide-on-drag, node dragging in stories
- **(canvas-ui)** Live, structure-aware node template editors
- **(canvas)** Make decoration teardown best-effort + add Canvas Boards story
- **(graph)** CompileCard avatar-initials label is single-line clipped
- **(maplibre)** Rename camera bridge events to input:camera:* taxonomy

### Refactors
- **(storybook)** Migrate use-case stories to GraphCanvasApp, drop _shared
- **(graph)** Drop PreviewTarget/PreviewTargetKind — use GraphNode/GraphEdge + GraphElementKind
- **(canvas-ui)** Organise into editors/ + views/ + shared/ folders
- Rename @invana/canvas-template-designer → @invana/canvas-designer + restructure
- **(canvas-store)** Rename @invana/canvas-core -> @invana/canvas-store (CanvasCore -> CanvasStore)
- **(canvas)** Re-export ColumnStore/DirtyBatcher from @invana/canvas-store (D1)

### Documentation
- **(api)** Regenerate TypeDoc API reference for theme + template surfaces
- Add roadmap + editor/state conventions; rename canvas-designer → canvas-template-designer
- Move design/plan docs from repo root into docs/
- Reposition as data investigation toolkit; state design notes; README/roadmap restructure
- Add canvas-state + designer-studio plans; wire @invana/canvas-state into instructions
- Consolidate canvas-state plan; add keyed data layers + groups; record decisions
- **(canvas-state)** Developer API (read/update, operations, history), data-write flow, heavy-update budgets
- Reconcile naming — canvas-state -> canvas-core kernel (state + events)
- Event taxonomy + state-ownership migration; 3-package pseudocode; plan updates

## [0.0.7] - 2026-06-20

### Features
- **(canvas)** Add animatePositions helper + easing-name registry
- **(graph)** Add OneShotPositionLayout base for deterministic layouts
- **(graph-layout-geometric)** Grid / snake / circular layouts
- **(storybook)** Full toolbar + layout switcher in StreamingData
- **(storybook)** Split streaming demo into cyclic / acyclic layout stories

### Bug Fixes
- **(canvas)** Fall back to WebGL on WebKit where pixi WebGPU crashes
- **(graph)** Clear connector arrow markers when toggled off
- **(graph-layout-d3-sankey)** Run via activeLayout, drop ribbon arrowhead
- **(storybook)** LesMiserables force layout collapsing to a ball
- **(graph-layout-elkjs)** Run ELK solve in a Web Worker
- **(storybook)** Streaming layout fixes + edge-type selector

### Refactors
- **(graph-layout-elkjs)** Port onto OneShotPositionLayout
- **(graph-layout-d3-hierarchy)** Port onto OneShotPositionLayout

### Documentation
- Document the layout family (README matrix + active packages)

## [0.0.6] - 2026-06-13

### Bug Fixes
- Element-level resize + status-bar label resolution + React 19 peer

## [0.0.5] - 2026-06-13

### Bug Fixes
- **(canvas-react)** Skip D3ForceLayout auto-fit on a stopped end

## [0.0.4] - 2026-06-10

### Features
- **(storybook)** Port GraphVisualiser into @invana/themes AppLayoutBase shell
- **(canvas-react)** Read-only property viewer driven by ClickViewBehaviour
- **(graph)** ColorByLabelBehaviour — unique colour per node/edge label
- **(canvas-react)** Reactive hover degree + magnet toggle in VisualiserApp
- **(canvas-react)** GraphHintBar — footer guidance line for interaction modes
- **(canvas-react)** Design-kit nav-item active styling for toolbar controls
- **(storybook)** GraphVisualiserApp status bar shows rendered + selection counts
- **(storybook)** GraphVisualiserApp hover shows display label + id
- **(canvas-react)** Selection-aware erase button replaces delete+clear
- **(canvas-react)** Data-driven toolbars — ToolbarItems renderer + section hooks
- **(canvas-react)** Extract reusable StatusBar component
- **(graph)** Theme-aware MiniMapLayer + visualiser contrast fixes
- **(canvas)** Serialisable config engine + layout registry + theme-agnostic GraphCanvas
- **(canvas-react)** Config-first <Canvas config={canvasOptions}> + migrate use-case stories
- **(canvas)** Add Canvas.redraw/refresh + Layer.redraw; wire re-render button
- **(canvas)** Engine message channel; replace GraphHintBar with CanvasMessageBar
- **(graph)** Focus-on-node centres + opt-in zoom instead of fit-to-fill
- **(storybook)** Add GraphModellerApp use-case story

### Bug Fixes
- **(graph)** Minimap mirrors resolved node/edge style + shape kind
- **(canvas-react)** Keep ContextMenuBehaviour onContextMenu reactive

### Refactors
- **(storybook)** Migrate graph stories to GraphCanvas + serialisable canvasOptions
- **(canvas-react)** Remove now-unused theme machinery after config-first migration
- **(behaviours)** Rename layerId → targetLayerId; move story behaviour options to config
- **(canvas-react)** Drop Engine* aliases & `engine` vars for package-namespaced names

### Documentation
- Unified GraphCanvas options + theme-agnostic refactor plan
- **(storybook)** Document GraphCanvas + serialisable-config story pattern

## [0.0.1] - 2026-06-07

### Features
- **(create-turbo)** Apply official-starter transform
- **(create-turbo)** Apply pnpm-eslint transform
- **(create-turbo)** Install dependencies
- Compound groups with collapse/expand + node resize
- Edge-types stories + quadratic pathStyle
- **(badges)** Edge badges + full node-badge upgrade
- **(stories)** Usecases/ showcase + supporting engine knobs
- **(graph-layer-bubble-sets)** New BubbleSetsLayer package
- **(canvas-react)** New @invana/canvas-react package + Storybook React framework
- **(canvas-react)** DragNodeBehaviour wrapper + canvas-react/ stories folder
- **(graph)** DegreeSizeBehaviour + unified NodeStyle.size
- **(canvas-react-ui-components)** New package + NodeStyleEditor v1
- **(canvas)** Add CompositeShape primitive (geometry + label parts at relative x,y)
- **(graph-datasets)** Add invana-code-kg dataset in property-graph shape
- **(stories)** Add elkjs-layout variant of the code-kg story
- **(graph-layout-elkjs)** ELK native orthogonal edge routing
- **(graph)** Right-click context-menu behaviour (node/edge/canvas)
- **(graph,canvas-react)** Visualiser + modeller stories, create/draw behaviours, full React wrappers
- **(canvas-ui)** Toolbar/controls actions track + use it in GraphVisualiser
- **(canvas-ui,storybook)** OS-theme-responsive visualiser + centered toolbar + fit-content
- **(canvas-ui,canvas-react)** React Flow-style Panel + hooks + preset controls
- **(graph)** Per-axis MiniMapLayer margin ({ x, y }) + bottom-align in visualiser
- **(graph,canvas-ui)** GraphLayer.clear() + Panel zIndex + sibling-overlay toolbars
- **(canvas-react)** Self-wiring ZoomPicker + useClearGraph
- **(graph,canvas-react)** Engine history + clipboard + 5 self-wiring toolbars
- **(canvas-react)** Make clear + node drag undoable via existing history
- **(graph)** Drag whole selection + fix pan/select gesture conflict
- **(canvas)** Grab cursor on press in DragPanBehaviour
- **(graph)** Shift-gated selection across click/brush/lasso
- **(canvas-react)** Per-option icons in OptionPicker / select-mode dropdown
- **(graph)** Default ClickSelect trigger to [] (plain click selects)
- **(canvas-react)** Real tooltips on all UI components via shared Tooltipped
- **(canvas-react)** Combine GraphVisualiser chrome into one toolbar
- **(canvas-react)** Edge routing picker (useEdgeType + EdgeTypePicker)
- **(canvas)** Finer BackgroundLayer grid defaults
- **(graph)** Figma-style selection-body drag in DragNodeBehaviour
- **(canvas-react)** Modeller drawing toolbar + node/edge property inspector
- **(canvas-react)** Reusable context-menu hook/overlay + graph story menus
- **(graph)** ClickInspectBehaviour + type-as-label inspector
- **(graph)** Raise hovered/selected set to front + render-order hit-test
- **(graph)** ResponsiveThemeBehaviour + theme toggle UI
- **(canvas-react)** Reusable Graph*ContextMenu + backing engine methods
- **(graph)** GraphStore owns interaction state; layers subscribe
- **(ci)** Add npm publish pipeline with lockstep versioning

### Bug Fixes
- Github actions fixes
- Github actions fixes for storybook build
- Github actions fixes for storybook build
- D3-contour stories — move node styling to layer resolvers
- D3-force stories — clean up node data, move styling to layer resolvers
- D3-hierarchy stories — labels missing, mis-placed, and broken size lookup
- Group drag, edge re-route on collapsed-group move, story polish
- **(stories)** Natural camera framing in LabelCollision + HoverActivate
- **(drag)** Node tracks cursor + drag survives cursor leaving canvas
- **(stories)** Correct mangled Shapes story titles (oncepts -> concepts)
- **(stories)** Make code-kg labels visible and fix label LOD wiring
- **(stories)** Left-align description label in Composite shape story
- **(graph)** Composite cards avoid edge overlap by default (router-based)
- **(graph)** Restore node opacity on state removal + hover/area-select fixes
- **(storybook)** Make GraphModeller theme-responsive like GraphVisualiser
- **(ci)** Make release.sh idempotent for first release

### Performance
- **(canvas)** Single-dispatcher hit-test pipeline (rbush + closest-wins)

### Refactors
- **(drag)** Decouple drag-lock from store.pinned + opt-in pinOnRelease
- **(stories)** Nest all stories under canvas/* + canvas/Concepts/ for core engine
- **(stories)** Lowercase Concepts in canvas story titles
- **(canvas-react-ui-components)** Schema-driven, engine-agnostic NodeStyleEditor
- **(canvas-react-ui-components)** Derive NodeStyleFields passthrough fields from NodeStyle
- **(canvas-ui)** Rename @invana/canvas-react-ui-components -> @invana/canvas-ui
- **(stories)** Move invana code-kg story into code-kg/ folder
- **(storybook)** Group canvas-react use-case stories under use-cases/
- **(canvas-react,canvas-ui)** Consolidate control UI into canvas-react
- **(canvas-react)** Rename CanvasControls → CanvasControlsToolbar

### Documentation
- **(stories)** Surface every Group / behaviour option in lil-gui
- **(stories)** Add Graph/Groups/AllOptions playground
- **(stories)** Graph-side loop-curve + loop-polyline stories
- **(stories)** Rename Graph/States/ to Graph/Etc/
- **(stories)** Group Behaviours stories under Select/ and Parallel/
- **(stories)** Effects.stories.ts for node + edge badges
- **(stories)** Bundle pathStyle demo + Cora subject-bundle subset
- **(claude)** Add standing rule — never commit/push unless explicitly asked
- **(graph)** Plan for store-owned interaction state
- **(graph)** Render coalescing + ER extensibility + pseudocode


